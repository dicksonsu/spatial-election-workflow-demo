# Derived Data

This folder is intended to store intermediate and processed datasets generated by the scripts.

These files are not included in the repository but are created when the workflow is run locally.
